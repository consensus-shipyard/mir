linters:
  enable:
    - gocyclo
    - revive
    - dupl
    - unconvert
    - goconst
    - gosec
    - gofmt
    - misspell

run:
  timeout: 2m

issues:
  exclude-rules:
    - path: .*\.pb\.mir\.go
      linters: [revive]
